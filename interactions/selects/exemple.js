module.exports = {
	customID: "exSelect",
	type: "text",
	/**
	 * @param {StringSelectMenuInteraction} interaction
	 * @param {Client} client
	 */
	runInteraction: async (client, interaction) => {
		// Le code ici
	},
};
