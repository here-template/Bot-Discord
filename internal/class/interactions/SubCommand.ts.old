import { ButtonInteraction } from "discord.js";
import { BotClient } from "../BotClient";
import { Command } from "./Command";

export type SubCommandInteraction = ButtonInteraction;

export default class SubCommand {
	admin?: boolean | undefined;
	run: (client: BotClient, interaction: SubCommandInteraction) => any = () => {};
	autoComplete?: (client: BotClient, interaction: SubCommandInteraction) => any = () => {};
}
