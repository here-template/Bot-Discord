//You need to import the classes you need (CustomClient, CommandInteraction, SubCommandRun)

// noinspection JSUnusedGlobalSymbols
export default {
    async run(client: CustomClient, interaction: CommandInteraction) {
        return interaction.reply({
            content: 'Une subCommand !'
        });
    }
} as SubCommandRun;